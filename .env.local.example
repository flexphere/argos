# LLM プロバイダ設定
# .env.local としてコピーして使ってください。
#
# 現状は `claude -p` をサブプロセスで呼び出す方式のみサポート。
#   - Claude Code が PATH に存在し、ログイン済みである必要あり
#   - API キー不要

# モデル指定（任意。指定しなければ `claude` のデフォルトモデル）
# LLM_MODEL=claude-sonnet-4-6
